# V0.4 FLYWAY BASELINE IMPLEMENTATION

**Date:** 2026-08-18  
**Status:** STATIC IMPLEMENTATION PASS — Flyway baseline Review Ready  
**Gate:** `V0.4_FLYWAY_BASELINE_REVIEW_READY`

## 1. Task understanding
Implement the Accepted V0.4 physical database baseline with Flyway for MySQL 8.4 only. JPA, repositories, services, APIs, frontend, business seed/cutover, Docker, and Testcontainers are out of scope.

## 2. Accepted input gate
PASS — `V0.4_DATABASE_DESIGN_ACCEPTED` was verified before implementation.

## 3. Git precheck
PASS — working tree was clean on `main`; HEAD was `83c4230 docs(db): accept V0.4 database design baseline`.

## 4. Existing migration inventory
`backend/src/main/resources/db/migration/` contained only `README.md`; no prior SQL migration or committed schema object conflicted with the accepted baseline.

## 5. Migration files created
- `backend/src/main/resources/db/migration/V001__v04_accepted_physical_baseline.sql`

## 6. Migration order
`V001` applies the accepted dependency sequence in one empty-repository baseline: lookup/foundation → identity/organization → file/content → quiz → EDU → weekly → competition → dashboard.

## 7. Physical table count
PASS — 50 `CREATE TABLE` statements; 50 InnoDB/utf8mb4 tables; no duplicate names.

## 8. Table reconciliation
PASS — the 50 implemented tables match the Accepted V0.4 ownership map and inventory; no extra table was created.

## 9. PK audit
PASS — every table uses `BIGINT NOT NULL AUTO_INCREMENT` primary key.

## 10. FK/action audit
PASS — 91 foreign keys implement the Accepted `RESTRICT`/`SET NULL` delete behavior and `CASCADE` updates. Historical facts do not cascade-delete.

## 11. Unique constraint audit
PASS — accepted identity, attempt, snapshot, period, attribution, source-selection, contribution-source, and aggregate uniqueness constraints are present.

## 12. CHECK constraint audit
PASS — 40 MySQL-compatible CHECK constraints implement the Accepted DB-enforced vocabularies and source/parent invariants. Application-enforced hierarchy, Weekly lifecycle/freeze/overlap, invitation consumption, and cross-row competition checks were not replaced with triggers.

## 13. Index audit
PASS — accepted query indexes are present; a UNIQUE constraint supplies equivalent B-tree coverage where the catalog labels the same key as an index. `idx_handbook_articles_fulltext` is included as physical candidate support only; Vietnamese search quality is not runtime-validated.

## 14. Composite Quiz FK evidence
PASS — `quiz_attempt_question_options` has `UNIQUE(attempt_question_id, id)` and `quiz_attempt_answers` has the RESTRICT composite FK `(attempt_question_id, selected_attempt_option_id)` to that pair. The selected option remains nullable.

## 15. Composite Weekly FK evidence
PASS — `weekly_question_options` has `UNIQUE(weekly_question_id, id)` and `weekly_submissions` has the RESTRICT composite FK `(weekly_question_id, selected_option_id)` to that pair.

## 16. Active-attempt guard evidence
PASS — `quiz_attempts.active_guard` is a stored generated `CASE WHEN status = 'ACTIVE' THEN 1 ELSE NULL END` column with unique `(account_id, quiz_config_id, active_guard)`.

## 17. Competition-source integrity evidence
PASS — canonical Quiz source uniqueness, one criterion per policy/source class, exactly-one contribution-source CHECK, source-type CHECK, and nullable source uniqueness are implemented.

## 18. Organization/history evidence
PASS — organization assignments and history use `DATETIME(3)`; history keeps the Accepted half-open-interval representation. Hierarchy and interval-overlap rules remain application-enforced as designed.

## 19. Exclusions
PASS — no BLOB, MySQL ENUM, H2, token/session persistence, learning-completion, personal-view, music-history, scheduler, or generic configuration table was introduced.

## 20. Technical seed result
No seed migration created. The Accepted baseline requires no concrete technical rows and forbids operational/business seed data.

## 21. Static SQL validation
PASS — 50 tables, 0 duplicates, 50 InnoDB/utf8mb4 declarations, no ENUM/BLOB, 91 FKs, 40 CHECKs, 34 UNIQUE definitions, both composite FKs, active guard, and competition integrity evidence were verified.

## 22. MySQL execution result
**NOT RUN — SAFE TEST DB UNAVAILABLE.** No local MySQL client or dedicated safe MySQL test schema was available. No H2, Docker, arbitrary connection, or invented credential was used.

## 23. Maven result
PASS — `backend/mvnw.cmd test` completed successfully. This is not a MySQL migration execution result.

## 24. Files changed
- `backend/src/main/resources/db/migration/V001__v04_accepted_physical_baseline.sql`
- `docs/PROJECT-STATUS.md`
- `CHANGELOG.md`
- `docs/reports/2026-08-18-V0.4-FLYWAY-BASELINE-IMPLEMENTATION.md`

## 25. Git diff review
PASS — `git diff --check` passed.

## 26. Open OIs
`OI-001`, `OI-003`, `OI-004`, `OI-005`, and `OI-015` remain Open.

## 27. Deviations/blockers
No schema deviation. MySQL execution remains unavailable; runtime migration validation is therefore not claimed.

## 28. Acceptance matrix
`AC-FLY-01` through `AC-FLY-24`: PASS for static implementation, except runtime MySQL execution is explicitly **NOT RUN** rather than PASS.

## 29. Next best task
Review the Flyway baseline before JPA implementation.
