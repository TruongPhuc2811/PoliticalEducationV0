# Cursor rules

Project rules use the modern `.cursor/rules/*.mdc` format.

Recommended starting context in Cursor:
- `@AGENTS.md`
- `@docs/PROJECT-STATUS.md`
- The Requirement/Version document for the current task.

Do not create a root `.cursorrules`; that format is intentionally not used here.
