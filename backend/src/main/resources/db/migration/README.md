# Flyway migrations

This folder is intentionally empty during repository skeleton phase.

Do not create the final production schema until V0.4 Database Design is approved.

Migration naming convention:

`V001__short_description.sql`
