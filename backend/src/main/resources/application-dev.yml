spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:political_education_dev}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Ho_Chi_Minh
    username: ${DB_USERNAME:pes_dev}
    password: ${DB_PASSWORD:change_me}

app:
  file-storage:
    root: ${FILE_STORAGE_ROOT:./data/uploads}
